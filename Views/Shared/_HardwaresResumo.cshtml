@model Hardware

<div class="col-sm-4 cold-md-4 col-lg-4">
    <p><img src="/@Model.ImagemThumbnailUrl" class="w-25 object-fit-contain img-fluid" /></p>
    <h3>Price: @Model.Preco.ToString("C")</h3>
    <h4>@Model.Nome</h4>
    <p>@Model.DescricaoCurta</p>
</div>